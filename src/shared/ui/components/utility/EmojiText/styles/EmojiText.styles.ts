/**
 * EmojiText Component Styles
 */

import { css } from '@emotion/react';

/**
 * Base emoji text container styles
 */
export const emojiTextContainerStyles = css`
  display: inline;
  
  .emoji {
    display: inline-block;
    margin: 0 2px;
    font-size: 1.2em;
    line-height: 1;
    vertical-align: middle;
  }
`;
