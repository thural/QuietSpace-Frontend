/**
 * Metric Card Component Index
 * 
 * Clean barrel exports for the Metric Card component.
 */

export { default as MetricCard } from './MetricCard';
export type { 
    IMetricChange,
    IMetricCardProps,
    IMetricCardState
} from './interfaces';
