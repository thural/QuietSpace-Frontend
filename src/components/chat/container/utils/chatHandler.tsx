export const handleChatException = (message) => {
    console.log("error on receiving message: ", message)
}

export const handleOnlineUser = (message) => {
    // TODO: implement online badge
}

export const handleChatDelete = (message) => {
    // TODO: mutate chat cache
}

export const handleDeleteMessage = (message) => {
    // TODO: mutate message cache
}

export const handleSeenMessage = (message) => {
    // TODO: mutate message cache
}

export const handleLeftChat = (message) => {
    // TODO: mutate mutate chat cache
}