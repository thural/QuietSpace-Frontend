/**
 * Query Executor Interface
 * 
 * Single responsibility: Query execution and coordination
 */

/**
 * Query Executor interface
 * @typedef {Object} IQueryExecutor
 * @property {function} executeQuery - Execute a query with intelligent caching and WebSocket integration
 * @property {function} executeMutation - Execute a mutation with optimistic updates
 * @property {function} executeInfiniteQuery - Execute infinite query with pagination
 */

// Export for JSDoc usage
export {};
