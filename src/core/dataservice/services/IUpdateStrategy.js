/**
 * Update Strategy Interface
 * 
 * Single responsibility: Data update strategies for WebSocket messages
 */

/**
 * Update strategy type
 * @typedef {string} UpdateStrategyType
 */

/**
 * Update Strategy interface
 * @typedef {Object} IUpdateStrategy
 * @property {function} apply - Apply update strategy to current data
 * @property {function} supports - Check if strategy is supported
 */

// Export for JSDoc usage
export {};
