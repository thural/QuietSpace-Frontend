import { createUseStyles } from "react-jss"

const styles = createUseStyles({
	cardboard: {
		columnGap: '1.2rem',
		columnCount: '2',
		columnWidth: '300px',
		marginTop: '1rem'
	}
});

export default styles
